apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.resources.appNames.v2 }}
  labels:
    app: {{ ..Values.resources.appNames.v2 }}
    chart: {{ template "more-helm-stuff.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 93
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: {{ ..Values.resources.appNames.v2 }}
    release: {{ .Release.Name }}
