apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.appNames.v2 }}
  labels:
    app: {{ .Values.service.appNames.v2 }}
    chart: {{ template "more-helm-stuff.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 93
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.service.appNames.v2 }}
    release: {{ .Release.Name }}
